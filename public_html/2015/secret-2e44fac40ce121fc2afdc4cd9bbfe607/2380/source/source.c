#include<stdio.h>

int main(){
    int a,b,j,k,t,n;

    scanf("%d", &t);

    for(j=0;j<t;j++){
        scanf("%d", &n);

        int a[j][k];

        for(k=0;k<n;k++){
            scanf("%d", &a[j][k]);
        }
    }

    for(j=0;j<t;j++){
        for(k=0;k<n;k++){
            float b[j][k];
        }
    }

    return 0;
}
