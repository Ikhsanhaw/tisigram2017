#include <stdio.h>
#include <string.h>

int main() {
	char kata[50];
	int count;

	scanf("d", &count);

	while (count--) {
		gets(kata);
		for (int j = 0; j < strlen(kata); j++) {
			int x = kata[i];
			if (x % 2 == 0) {
				printf("%c", kata[i]); } }
		printf("\n"); }

		return 0; }
			
