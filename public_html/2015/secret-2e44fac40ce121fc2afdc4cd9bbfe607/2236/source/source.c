#include <stdio.h>
   int main(){
       int A;
       int B;
       int H;
       scanf("%d" "%d" "%d", &A, &B ,&H);
       H=(H/B)+A;
       if (A<B){
          printf("mustahil\n");
       }
       else{
          printf("%d\n", H);
          }
}
