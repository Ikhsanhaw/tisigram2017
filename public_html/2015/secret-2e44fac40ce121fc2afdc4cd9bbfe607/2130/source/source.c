#include<stdio.h>

int main(){
    int N, i, ascii;
 
    scanf("%d", &N);
    for(i=0; i<N; i++){
             scanf("%d", &ascii);
             printf("%c", ascii);
             }

             return 0;
    }
